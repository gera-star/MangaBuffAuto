# V9 — Card Stats V8 + robust reader scroll

Restores the V11.1 reader while fixing the scroll path: the bot now detects the actual scrollable reader container instead of assuming `window`, and keeps the 1x/2x multiplier. Card Statistics V8 and edge gestures are preserved.
